import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import ChatIndex from './src/Chat';
const App = () => {
  return <ChatIndex />;
};

export default App;

const styles = StyleSheet.create({});
